import SwiftUI

@main
struct WidgetPreviewHost: App {
    var body: some Scene {
        WindowGroup("Widget Preview Host") {
            Text("Use the StylePreviews.swift Canvas to preview routing controls.")
                .padding(24)
        }
    }
}
